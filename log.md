# Update Log

## 2026-07-26
* **Creation**: Established the bundle. `certs/retiring` (24) and
  `certs/renamed` (16) sourced from Salesforce's official 2027
  Certification Retirements FAQ and Certification Name Changes FAQ.
  `products` (92) sourced from the renameforce.com community dataset.
  Generated by an agent (`claude-sonnet-5/claude-code`); no human review
  pass yet — see index.md's Provenance and trust section.

## 2026-07-26 (verification pass)
* **Verification**: 7 automated fact-check agents (Haiku, one per product category group plus one for all 40 certifications) independently searched for a second source per entry. 123 of 132 concepts moved from `unverified` to `machine-confirmed` trust tier (see each concept's second `sources` entry and `verified` field). 4 discrepancies flagged, not yet resolved:
  - `products/audience-studio.md` (Audience Studio): Timeline shows end date of 2023, but Audience Studio was actually retired February 1, 2024 (not 2023).
  - `products/salesforce-functions.md` (Salesforce Elastic Services): CONFLICT: Timeline shows product ended January 2024, but search confirms Salesforce Functions retiring January 31, 2025; also unclear if current_name should still be 'Salesforce Elastic Services' if product is being discontinued.
  - `products/nonprofit-cloud.md` (Agentforce 360 for Nonprofits): Nonprofit Cloud was renamed to 'Agentforce Nonprofit', not 'Agentforce 360 for Nonprofits' as stated in JSON.
  - `products/einstein-automation-studio.md` (Flow Builder): Timeline data shows Visual Process Manager (2010-2011), but search sources don't confirm this name exists; progression shown is Visual Workflow→Lightning Flow→Flow Builder.
  A handful of entries could not be independently confirmed and remain `unverified` — see renames.jsonl's `verified: false` entries.

## 2026-07-27 (second verification pass — official sources only)
* **Verification**: re-checked all 9 concepts that came out of the first pass without a clean `verified` status (the 4 flagged + 5 unverified) using a fresh round of Haiku subagents, run sequentially rather than in parallel, each instructed to discover via `WebSearch`/Exa (never scraping Google's results page directly — a real bot-detection/ban risk) and confirm via official Salesforce domains (`help.salesforce.com`, `salesforce.com`, `developer.salesforce.com`) rather than leaning on renameforce.com or general blogs. Browser automation (Playwright/Chrome DevTools MCP) was reserved strictly for rendering `help.salesforce.com`'s Lightning/Aura pages, which don't return content via plain fetch.
* **Corrected in place** (now `verified: true`, second official source added):
  - `products/audience-studio.md` — retirement date corrected 2023 → 2024-02-01, per Salesforce's own Help article.
  - `products/salesforce-functions.md` — retirement date corrected 2024-01 → 2025-01-31, per Salesforce Help + developer release notes.
  - `products/nonprofit-cloud.md` — current name corrected "Agentforce 360 for Nonprofits" → "Agentforce Nonprofit," per Salesforce's own announcement page (re-read directly rather than taken on faith from the first pass's citation).
  - `products/code-analyzer.md` — fixed an internal date-overlap bug (CLI Scanner/SFDX Scanner/Code Analyzer all showed 2022 as a start year); corrected to 2020 / 2021 / 2024-06 per official release notes.
* **Flag resolved as a false positive**:
  - `products/einstein-automation-studio.md` — the first pass's flag on "Visual Process Manager" turned out wrong. A Salesforce press release from January 2010 names the product directly. Original data was correct; now `verified: true`.
* **Newly disputed** (real, sourced concerns — not just unconfirmed):
  - `products/marketing-cloud-plus.md` — no source, official or community, corroborates "Marketing Cloud Engagement Einstein Engage Plus on Core" as a real name. The entry looks likely fabricated or badly garbled in the underlying dataset.
  - `products/salesforce-billing.md` — this looks like product succession (Salesforce Billing being phased out as Revenue Cloud Billing arrives), not a rename of the same product. The actual 2025 rename event was "Revenue Cloud" → "Agentforce Revenue Management," a different product than this entry names.
* **Still genuinely unresolved** (searched, found nothing either way):
  - `products/salesforce-event-bus.md` — no official record of a discrete 2024 rename event to "Salesforce Event Bus."
  - `products/identity.md` — no official record of a 2013 origin or 2021 rename to "Customer Identity and Access Management."
* Net effect: 128 of 132 concepts are now `machine-confirmed`, 2 are flagged as actively disputed, 2 remain unconfirmed either way. No corrections were resubmitted to renameforce.com's form for this pass — the first pass's 4 submissions stand; resubmitting again so soon would look like spam, not a correction.

## 2026-07-27 (third pass — auditing source quality, all 40 certs + 42 products)
* **Audit**: a check of every concept's second `sources` entry found that "machine-confirmed" had been overstating trust quality. **All 40 certifications' corroboration cited community aggregators** (apexhours.com, salesforcetrail.com) despite the primary source already being Salesforce's own retirement/rename FAQs. On the product side, 43 of 92 cited blog corroboration and 5 had no second source at all.
* **Certs (40 of 40)**: re-fetched both canonical FAQ pages live via browser automation (`help.salesforce.com/s/articleView?id=005360289` for retirements, `id=005298915` for renames) rather than trusting the original scrape or a blog's summary of it. Every certification's name and date matched exactly — zero drift. All 40 concepts' `verified.by` updated to reflect this direct re-check; the blog corroboration is kept but no longer the basis for the trust claim. (This same live re-fetch also independently reconfirmed the second-pass correction to `nonprofit-cloud.md`: the official page names it "Salesforce Certified Agentforce Nonprofit Consultant," not "...360...".)
* **Products (42 re-researched)**: batched into 7 subagents grouped by category (Sales, Platform, Marketing, Industries, Analytics/Commerce/Automation, Service/Data, Integration/AI), each restricted to official-domain discovery only. Results:
  - **25 confirmed clean** against official Salesforce/partner sources, several with minor date corrections (SteelBrick's 2016 intermediate branding, AppExchange→AgentExchange's March vs April 2026 date, Marketing Cloud Intelligence's October vs June 2022 date).
  - **4 corrected with substantially wrong data**: `heroku-key-value-store.md` (dates off by ~8 years), `nonprofit-success-pack.md` (renamed 2013, not 2016), `omnistudio.md` (wrong predecessor name — "Vlocity Digital Interaction Platform," not "Vlocity OmniStudio"), `crm-analytics.md` (the longest chain in the bundle — 4 of 9 claimed name stages could not be found anywhere and were cut; only Wave→Einstein Analytics→Tableau CRM→CRM Analytics survived independent confirmation).
  - **4 newly disputed** (a real, sourced conflict, not just an absence of one): `web3.md` (renamed to "NFT Management," not "Salesforce Web3," which is a platform umbrella), `commerce-cloud.md` ("Commerce GPT" as a named product stage looks like an AI-feature announcement, not a rename), and `service-cloud.md`/`field-service.md` — both show a genuine three-way conflict: Winter 2026 release notes still say "Service Cloud"/"Field Service," but this bundle's own live-fetched certification-rename FAQ (see above) explicitly renames the matching certifications to "Agentforce Service Consultant" and "Agentforce Field Service and Operations Consultant." Flagged for a human to reconcile rather than guessed at.
  - **11 remain genuinely unconfirmed**: no official source found either way for `revenue-cloud.md` (partial — only some stages confirmed), `experience-cloud.md` (final stage unconfirmed), `lightning-components.md`, `embedded-messaging.md` (middle stage), `salesforce-data-cloud.md` (only the 2025-10-14 Data Cloud→Data 360 date confirmed precisely, earlier chain not re-verified), `mulesoft-anypoint.md`, `mulesoft-flow-integration.md`, `mulesoft-omni-gateway.md` (current name confirmed, rename date not), `prompt-builder.md`, `sales-emails-and-alerts.md` (real launch name found — "Sales Cloud Engage" — but the claimed "SalesEdge" codename wasn't corroborated).
  - **2 confirmations reverted as invalid**: a subagent researching `agentforce-coworker.md` and `einstein-ai.md` initially reported "CONFIRMED," but its only citation was renameforce.com itself — the same dataset this bundle is independently checking, so that isn't real corroboration. Both reverted to unverified rather than counted as confirmed on a circular source.
* **Process note**: this pass also caught and fixed its own bug mid-run — several products re-marked "disputed" or "uncertain" had inherited `verified: true` from the original (looser) first pass and weren't automatically downgraded when a note was added; a follow-up correction explicitly reset all 15 affected entries' `verified` field to `false`.
* Net effect across the whole bundle: **40 of 40 certs directly re-confirmed against live official sources**; of 92 products, **71 machine-confirmed against official-domain sources** (up from a mix of official and blog-only), **8 disputed** (a real conflict found, not just an absence of proof), **13 genuinely unconfirmed**. No corrections were resubmitted to renameforce.com's form this round.
